Source: flexa
Section: gnome
Priority: optional
Maintainer: caue <caue@fedora>
Build-Depends: debhelper-compat (= 13),
               meson,
               ninja-build,
               gettext,
               desktop-file-utils,
               libappstream-glib-dev,
               python3-dev,
               libgtk-4-dev (>= 4.10.0),
               libadwaita-1-dev (>= 1.5.0),
               libgirepository1.0-dev,
               libglib2.0-dev,
               python3-gi
Standards-Version: 4.7.0
Homepage: https://github.com/eucaue/flexa
Rules-Requires-Root: no

Package: flexa
Architecture: linux-any
Depends: ${shlibs:Depends},
         ${misc:Depends},
         python3,
         python3-gi,
         python3-cairo,
         python3-gi-cairo,
         gir1.2-gtk-4.0,
         gir1.2-adw-1,
         libgtk-4-1 (>= 4.10.0),
         libadwaita-1-0 (>= 1.5.0),
         python3-win2xcur
Description: Convert Windows cursor themes to Linux format
 Flexa is a simple GNOME application that converts Windows cursor
 themes (.cur and .ani files) to the Linux XCursor format. It
 provides a clean, intuitive interface following the GNOME Human
 Interface Guidelines.
 .
 Add multiple cursor folders at once — either through the file
 picker or by dragging and dropping them into the window. Flexa
 will scan each folder for cursors and convert them all with a
 single click. When done, a toast notification lets you open the
 output directory instantly.
 .
 The output directory is configurable in Preferences and defaults
 to ~/.local/share/icons, so converted themes are immediately
 available in GNOME Settings → Appearance.
